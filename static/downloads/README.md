Coloque aqui o APK gerado (`motorista_app/build/app/outputs/flutter-apk/app-release.apk`)
com o nome exato `motorista-app.apk`. É o arquivo servido em
`https://sync.columbiamachine.com.br/motorista/app`.

Este é um placeholder — o arquivo `.apk` em si não deve ser versionado no git
(é binário e muda a cada build); suba-o direto para o servidor de produção.
